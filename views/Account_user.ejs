<%- include("layouts/header") %>

<div class="main_container">
  <!-- <div class="card">
    <% if(message.length >0){ %>
        <div id="alerty" class="alert alert-success">
  
       <button id="msg"><%= message %></button> 
        <button id='close' onclick='this.parentNode.parentNode.removeChild(this.parentNode); return false;'>x</button>
    </div><br>
  <% } %> -->
        <!-- <% if (user.image !='err') { %>
            <img class="card-img-left" src="/images/user-pic/<%= user.image %>" alt='not found'style="height:120px;width:120px;border-radius:50%" >
        <% } else { %>
            <img class="card-img-left" src="/images/<%= user.gender %>.png" style="height:120px;width:120px;border-radius:50%" >
            <% } %> -->
            <div class="card-body">
                <h2 class="card-title" style="color:white">Hi <%= user.username %> !</h2>
                <ul>
                    <li class="card-text">
                        <p><%= user.bio %></p>
                    </li>
                    <% if (user.Courses.length === 0) { %>
                    <li class="card-text"><b>Courses:</b> You are not enrolled in any courses yet </li>
                    <% } else { %>
                    <li class="card-text"><h4>Courses:</h4>
                        <ul>
                            <% user.Courses.forEach(course => { %> 
                            <li class="card-text"><a id="tl" href="/materials" > <%= course %></a></li>
                            <% }); %>
                        </ul>
                    </li>
                    <% } %>
                    <br>
        
            <a href="/edit-profile/<%= user.username %>"id="btnn" class="btn btn-primary">Edit</a>

                </ul>
            </div>
     
  </div>
</div>
            

<%- include("layouts/footer") %>


